<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Barberia2.BarberosSelector"
             Title="BarberosSelector">
    <VerticalStackLayout>
        <Grid ColumnDefinitions="*,*,*">
            <Button Text="Registros" Clicked="Registrosbtn"></Button>
            <Button Grid.Column="1" Text="Descansos" Clicked="Descansosbtn"></Button>
            <Button Grid.Column="2" Text="Historial" Clicked="Historicobtn"></Button>
        </Grid>
    </VerticalStackLayout>
</ContentPage>