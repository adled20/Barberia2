<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Barberia2.Seleccion"
             Title="Seleccion">
    <VerticalStackLayout>
        <Grid ColumnDefinitions="*,*,*,*">
            <Button Margin="10,0,10,0"  Clicked="Combos" Text="Combos"></Button>
            <Button Margin="0,0,10,0" Grid.Column="1" Clicked="Cortes" Text="Cortes"></Button>
            <Button Margin="0,0,10,0" Grid.Column="2" Clicked="Productos" Text="Productos"></Button>
            <Button Margin="0,0,10,0" Grid.Column="3" Clicked="Planes" Text="Planes"></Button>
        </Grid>
    </VerticalStackLayout>
</ContentPage>